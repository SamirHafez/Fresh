<controls:PageBase
    x:Class="Fresh.Windows.Views.TVShowPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fresh.Windows.Views"
    xmlns:controls="using:Fresh.Windows.Shared.Controls"
    xmlns:prism="using:Microsoft.Practices.Prism.Mvvm"
    prism:ViewModelLocator.AutoWireViewModel="True"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:designTime="using:Fresh.Windows.Shared.DesignTime"
    mc:Ignorable="d">

    <d:Page.DataContext>
        <designTime:TVShowPageViewModel/>
    </d:Page.DataContext>

    <Grid>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ScrollViewer Margin="39,10" VerticalScrollBarVisibility="Disabled" Grid.Row="1" HorizontalScrollMode="Enabled" VerticalScrollMode="Disabled" VerticalContentAlignment="Center" HorizontalScrollBarVisibility="Auto">
            <StackPanel Orientation="Horizontal">
                <GridView
            AutomationProperties.AutomationId="ItemGridView"
            AutomationProperties.Name="Items In Group"
            TabIndex="1"
            Grid.RowSpan="2"
            ItemsSource="{Binding Seasons}"
            IsItemClickEnabled="True"
            SelectionMode="None"
            IsSwipeEnabled="false">
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Poster}" Width="350" Margin="0, 4, 10, 0" AutomationProperties.Name="{Binding Number}"/>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                    <GridView.Header>
                        <StackPanel Width="480" Margin="0,4,14,0">
                            <TextBlock Text="{Binding Network}" Margin="0,0,0,20" Style="{StaticResource SubheaderTextBlockStyle}" MaxHeight="60"/>
                            <Image Source="{Binding Images.Poster}" Height="400" Margin="0,0,0,20" Stretch="UniformToFill" AutomationProperties.Name="{Binding Title}"/>
                            <TextBlock Text="{Binding Overview}" MaxLines="5" Margin="0,0,0,0" Style="{StaticResource BodyTextBlockStyle}"/>
                        </StackPanel>
                    </GridView.Header>
                    <GridView.ItemContainerStyle>
                        <Style TargetType="FrameworkElement">
                            <Setter Property="Margin" Value="52,0,0,2"/>
                        </Style>
                    </GridView.ItemContainerStyle>
                </GridView>

                <StackPanel Orientation="Vertical" Grid.Row="1">
                    <TextBlock Style="{StaticResource HeaderTextBlockStyle}" HorizontalAlignment="Left">top episodes</TextBlock>
                    <Grid Height="300" Width="600">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Margin="10" Source="{Binding TopEpisodes[0].Screen}" Stretch="UniformToFill" AutomationProperties.Name="{Binding Title}"/>
                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60*"></RowDefinition>
                                <RowDefinition Height="40*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Image Grid.Column="1" Grid.Row="0" Margin="10" Source="{Binding TopEpisodes[1].Screen}" Stretch="UniformToFill" AutomationProperties.Name="{Binding Title}"/>
                            <Image Grid.Column="1" Grid.Row="1" Margin="10" Source="{Binding TopEpisodes[2].Screen}" Stretch="UniformToFill" AutomationProperties.Name="{Binding Title}"/>
                        </Grid>
                    </Grid>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{Binding Title}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
    </Grid>
</controls:PageBase>
